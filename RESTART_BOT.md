# Инструкция по перезапуску бота

## Проблема: треки не добавляются

Если треки не добавляются, выполните следующие шаги:

### 1. Остановите все экземпляры бота:
```bash
pkill -9 -f "python.*main.py"
```

### 2. Проверьте, что все процессы остановлены:
```bash
ps aux | grep "python.*main.py" | grep -v grep
```
(Должно быть пусто)

### 3. Запустите бота заново:
```bash
cd /Users/nikitapetrov/playRoom
source venv/bin/activate
python main.py
```

### 4. Проверьте логи:
В консоли должны появиться сообщения:
- `🧩 confirm_track: room_id=..., title=..., file_hash=...`
- `🔐 Пользователь ... является админом/владельцем: True/False`
- `🔐 Модерация включена: True/False`
- `✅ Трек ... добавлен в комнату ..., результат rpush: ...`
- `📊 Всего треков в комнате после добавления: ...`

### 5. Если треки все еще не добавляются:
- Проверьте логи на наличие ошибок
- Убедитесь, что Redis работает: `redis-cli ping`
- Проверьте, что вы действительно админ комнаты

## Важно:
- Убедитесь, что запущен только ОДИН экземпляр бота
- Если бот запущен на сервере, остановите его там перед локальным запуском
- Проверьте, что используете правильный токен бота в .env
