# Настройка автоматического деплоя

## Автоматические проверки ✅

Автоматические проверки кода **уже работают** и запускаются при каждом push:
- ✅ Проверка синтаксиса
- ✅ Проверка импортов  
- ✅ Линтинг
- ✅ Проверка безопасности
- ✅ Тесты

## Настройка автоматического деплоя на сервер

Для включения автоматического деплоя при пуше в `main`/`master` нужно настроить GitHub Secrets.

### Шаг 1: Настройка GitHub Secrets

Перейдите в настройки репозитория: **Settings → Secrets and variables → Actions**

Добавьте следующие секреты:

1. **`DEPLOY_ENABLED`** = `true`
   - Включает автоматический деплой

2. **`DEPLOY_HOST`** = `your-server-ip-or-domain`
   - IP адрес или домен вашего сервера
   - Пример: `192.168.1.100` или `bot.example.com`

3. **`DEPLOY_USER`** = `root` (или другой пользователь)
   - Пользователь для SSH подключения

4. **`DEPLOY_SSH_KEY`** = `-----BEGIN OPENSSH PRIVATE KEY-----...`
   - Приватный SSH ключ для подключения к серверу
   - Полный ключ включая заголовки

5. **`DEPLOY_PORT`** = `22` (опционально)
   - Порт SSH, по умолчанию 22

### Шаг 2: Подготовка SSH ключа

На сервере создайте SSH ключ для деплоя:

```bash
# На сервере
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy
```

Добавьте публичный ключ в `authorized_keys`:

```bash
cat ~/.ssh/github_deploy.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Скопируйте приватный ключ в GitHub Secrets как `DEPLOY_SSH_KEY`:

```bash
cat ~/.ssh/github_deploy
# Скопируйте весь вывод включая BEGIN и END строки
```

### Шаг 3: Проверка прав доступа

Убедитесь, что:
- Пользователь имеет права на запись в `/var/playroom`
- Может выполнять `start_bot.sh`
- Может устанавливать пакеты через pip

### Шаг 4: Тестирование

После настройки секретов:
1. Сделайте push в ветку `main` или `master`
2. Проверьте workflow в GitHub Actions
3. Бот должен автоматически обновиться и перезапуститься

## Что происходит при деплое

1. ✅ **Проверки CI** - сначала запускаются все проверки кода
2. 📦 **Создание пакета** - код упаковывается в tar.gz
3. 📤 **Загрузка на сервер** - файлы копируются через SCP
4. 🔄 **Обновление зависимостей** - установка/обновление pip пакетов
5. 🚀 **Перезапуск бота** - автоматический перезапуск через `start_bot.sh`
6. ✅ **Проверка статуса** - проверка, что бот запустился

## Безопасность

- ⚠️ **Никогда не коммитьте SSH ключи в репозиторий**
- ✅ Используйте только GitHub Secrets для хранения ключей
- ✅ Используйте отдельный SSH ключ только для деплоя
- ✅ Ограничьте права доступа ключа (если возможно)

## Ручной деплой

Если автоматический деплой не настроен, можно развернуть вручную:

1. Скачайте артефакт `bot-deployment` из GitHub Actions
2. Загрузите на сервер
3. Распакуйте: `tar -xzf bot.tar.gz`
4. Установите зависимости: `pip install -r requirements.txt`
5. Перезапустите: `./start_bot.sh`

## Troubleshooting

### Деплой не запускается

- Проверьте, что `DEPLOY_ENABLED=true` в Secrets
- Проверьте логи workflow в GitHub Actions
- Убедитесь, что все секреты настроены правильно

### Ошибка подключения SSH

- Проверьте правильность `DEPLOY_HOST` и `DEPLOY_PORT`
- Убедитесь, что SSH ключ правильный и добавлен в `authorized_keys`
- Проверьте firewall на сервере

### Бот не запускается после деплоя

- Проверьте логи: `tail -f /var/playroom/bot.log`
- Убедитесь, что зависимости установлены: `pip list`
- Проверьте права на файлы: `chmod +x start_bot.sh`
