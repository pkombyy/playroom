#!/bin/bash
# Быстрое применение конфигураций x-ui из файлов

set -e

GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
BLUE='\033[0;34m'
NC='\033[0m'

SCRIPT_DIR="/var/playroom"
ROUTING_CONFIG="${SCRIPT_DIR}/xui_advanced_routing.json"
DNS_CONFIG="${SCRIPT_DIR}/xui_advanced_dns.json"

echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
echo -e "${BLUE}  Применение конфигураций x-ui${NC}"
echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
echo ""

# Проверка файлов
if [ ! -f "$ROUTING_CONFIG" ]; then
    echo -e "${RED}❌ Файл routing конфигурации не найден: $ROUTING_CONFIG${NC}"
    exit 1
fi

if [ ! -f "$DNS_CONFIG" ]; then
    echo -e "${RED}❌ Файл DNS конфигурации не найден: $DNS_CONFIG${NC}"
    exit 1
fi

# Проверка валидности JSON
echo -e "${YELLOW}🔍 Проверка валидности JSON...${NC}"
if ! python3 -m json.tool "$ROUTING_CONFIG" > /dev/null 2>&1; then
    echo -e "${RED}❌ Ошибка в routing конфигурации (невалидный JSON)${NC}"
    exit 1
fi

if ! python3 -m json.tool "$DNS_CONFIG" > /dev/null 2>&1; then
    echo -e "${RED}❌ Ошибка в DNS конфигурации (невалидный JSON)${NC}"
    exit 1
fi

echo -e "${GREEN}✅ JSON файлы валидны${NC}"
echo ""

# Копирование в /tmp для удобства
cp "$ROUTING_CONFIG" /tmp/xui_routing_config.json
cp "$DNS_CONFIG" /tmp/xui_dns_config.json

echo -e "${GREEN}✅ Конфигурации скопированы в /tmp/${NC}"
echo ""

# Вывод содержимого для копирования
echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
echo -e "${BLUE}  Routing конфигурация${NC}"
echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
echo ""
cat "$ROUTING_CONFIG" | python3 -m json.tool
echo ""

echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
echo -e "${BLUE}  DNS конфигурация${NC}"
echo -e "${BLUE}═══════════════════════════════════════════════════════${NC}"
echo ""
cat "$DNS_CONFIG" | python3 -m json.tool
echo ""

echo -e "${YELLOW}📋 Инструкции:${NC}"
echo -e "${YELLOW}1. Войдите в веб-панель x-ui${NC}"
echo -e "${YELLOW}2. Перейдите в Settings → Routing${NC}"
echo -e "${YELLOW}3. Скопируйте routing конфигурацию выше и вставьте${NC}"
echo -e "${YELLOW}4. Перейдите в Settings → DNS${NC}"
echo -e "${YELLOW}5. Скопируйте DNS конфигурацию выше и вставьте${NC}"
echo -e "${YELLOW}6. Нажмите Save и перезапустите x-ui${NC}"
echo ""

# Проверка статуса x-ui
if command -v x-ui &> /dev/null; then
    echo -e "${YELLOW}Текущий статус x-ui:${NC}"
    x-ui status || echo -e "${YELLOW}⚠️  x-ui не запущен${NC}"
fi

echo ""
echo -e "${GREEN}✅ Готово!${NC}"
